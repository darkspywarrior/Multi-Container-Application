global:
  scrape_interval: 15s

scrape_configs:
- job_name: fingerprint-service
  metrics_path: /actuator/prometheus
  static_configs:
  - targets:
    - fingerprint-service:8082

- job_name: prometheus
  static_configs:
  - targets:
    - prometheus:9090
